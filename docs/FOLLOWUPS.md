# Engineering Follow-ups

Open follow-up items deferred from completed plans and their reviews (items
1–8 came from the engineering-hygiene plan,
`docs/superpowers/plans/2026-08-25-engineering-hygiene.md`; later items name
their own source plan). Each item names the condition that should trigger it.
Items are removed from this table when fixed.

| #   | Source     | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Code ref                                                                       | Trigger to fix                                             |
| --- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------- |
| 1   | T11 review | **SSE-split corruption**: `buffer.split(/(?=data:/)` also splits _inside JSON string values_. Any delta payload containing a literal `data:` cuts mid-token; both fragments fail `JSON.parse` and vanish through the silent `catch {}` blocks, so streamed text is silently truncated. Minimal repro (verified): for the event `data: {"choices":[{"delta":{"content":"see data: below"}}]}\n\n`, the split yields `"data: {\"choices\":[{\"delta\":{\"content\":\"see"` and `"data: below\"}}]}"` — neither parses, and the whole delta is lost. Fix by splitting on event boundaries (`\n\n`) or parsing with a streaming-aware buffer instead of a lookahead regex. | `lib/providers/custom-gateway.js:87` (drops at `:104`, `:114`, `:131`, `:140`) | Next touch of the streaming parser                         |
| 2   | T10 review | **CWS commented block hazards** in `release.yml`: the disabled publish step would run unpinned `npx chrome-webstore-upload-cli` and uploads `--source dist/*.zip`, which is a multi-archive glob risk once more than one release zip exists in `dist/`. Rewrite (pin the CLI version, upload exactly one artifact) is required **at enablement time**, not before.                                                                                                                                                                                                                                                                                                     | `.github/workflows/release.yml` commented block (~line 52)                     | When the commented CWS publish block is enabled            |
| 3   | T12        | **upload-artifact may be empty on CI failure** if the reporter configuration changes again: the artifact path must match what the configured reporter actually writes. Currently pinned to `test-results/` (Playwright default); recheck the path whenever the Playwright reporter config changes (e.g. adding an HTML reporter writes `playwright-report/`).                                                                                                                                                                                                                                                                                                          | `.github/workflows/ci.yml` upload-artifact step                                | When adding/changing the Playwright reporter               |
| 4   | T8/T9/T10  | **Branch protection + first real CI/tag-push observations**: user-side checklist (required-check names, tag-push dry runs) lives in the task-8 / task-9 / task-10 reports under `.superpowers/sdd/2026-08-25-engineering-hygiene/`. Complete those observations on GitHub after merge; no code change scheduled here.                                                                                                                                                                                                                                                                                                                                                  | task-8/task-9/task-10 reports                                                  | First real CI run / first tagged push after merge          |
| 5   | T12 review | **Jest positive-scoping convention**: `jest.config.js` sets `testMatch: ["<rootDir>/tests/**/*.test.js"]`, so a future `*.spec.js` file placed under `tests/` silently never runs (and vice versa, e2e specs are invisible to Jest). Convention: unit tests = `*.test.js` under `tests/`; browser E2E = `*.spec.js` under `e2e/`. Do not mix the suffixes.                                                                                                                                                                                                                                                                                                             | `jest.config.js`                                                               | Any new test file (follow the convention)                  |
| 6   | T12 review | **Zero retries on blocking CI smoke**: `playwright.config.js` defaults to `retries: 0`, so one flaky extension-launch hiccup fails the blocking gate. Consider `retries: process.env.CI ? 1 : 0` as a hedge once real CI observations land.                                                                                                                                                                                                                                                                                                                                                                                                                            | `playwright.config.js`                                                         | After first real CI observations (item 4)                  |
| 7   | T12 review | **Fixture-server leak on launch failure**: in the selection spec, `serveFixtureHtml()` resolves before `launchWithExtension()`; if the launch throws, `server.close()` in the `finally` is never reached because the server handle is only bound inside the `try`. Hoist the launch into the guarded region or close the server in a `catch` so failed launches don't leak a listening handle.                                                                                                                                                                                                                                                                         | `e2e/smoke.spec.js:11-13`                                                      | Next touch of `e2e/extension.fixtures.js` / the smoke spec |
| 8   | composer-adapters (manual test) | **`e2e/extension.fixtures.js` can't load the extension with 5+ `commands.suggested_key` entries — avoid a 5th until this is understood.** While trying `manifest.json` with 7 declared commands (`_execute_action`, `quick_ask`, `quick_rephrase`, `quick_translate`, `quick_fix_grammar`, `quick_summarize`, `quick_explain`), 3 with a `suggested_key` beyond Chrome's documented 4-auto-bind limit, `launchWithExtension()`'s `context.waitForEvent("serviceworker")` never resolved — waited 75s to rule out slowness, reproduced 3x with an ABA pattern (4 keys → instant load; 5 keys → hang; back to 4 → instant load) to rule out flakiness. [Chrome's own docs](https://developer.chrome.com/docs/extensions/reference/api/commands) say exceeding 4 should degrade gracefully (extension loads, extras just don't auto-bind), so this is most likely specific to Playwright's unbranded `channel: "chromium"` test build rather than real Chrome — unconfirmed, since stable Chrome dropped `--load-extension` and there was no way to verify via a real "Load unpacked" flow from here. **Currently moot**: `manifest.json` is back to exactly 4 `suggested_key` entries (Alt+A/R/T/F) by design, so the e2e suite loads fine. Re-investigate before ever adding a 5th. | `manifest.json` commands, `e2e/extension.fixtures.js:12-21`                   | Before declaring a 5th `commands.suggested_key`             |
| 9   | provider-registry-and-onboarding plan, manual smoke test | **Manual Chrome smoke test never run.** Plan Task 8 Step 8 required loading the unpacked extension in real Chrome with real Gemini + Anthropic API keys to confirm: (1) fresh install auto-opens Settings; (2) a non-Vietnamese Chrome UI language sets the detected primary-language default; (3) pasting a real key and blurring the field auto-validates; (4) picking a `-custom` model auto-expands the Advanced disclosure; (5) the Anthropic key field/tooltip/Validate flow works end-to-end. All of `npm run verify` + `npx playwright test` + `bash scripts/publish.sh` passed, but none of these 5 exercise a real browser load or a live API key. | `background/service-worker.js` (onInstalled), `settings.js` (`detectSupportedLocale`, auto-validate, Advanced auto-expand), `lib/providers/anthropic.js` | Before/soon after next Chrome Web Store upload of this version |
| 10  | provider-registry-and-onboarding plan, T1 review | **Residual prefix-derivation for `-custom` models**: `lib/ai-service.js` still does `activeModel.split("-")[0]` to derive a provider id for `-custom` models, instead of going through `AI_PROVIDERS`/`getProviderByModel()` like every other lookup. Not currently wrong — every `-custom` id is literally `<providerId>-custom` by convention (verified for google/openai/groq/anthropic) — but it's a second, unenforced place a future provider addition could get out of sync with the registry. | `lib/ai-service.js` (`activeModel.split("-")[0]`) | Next time a provider's id doesn't match its `-custom` model id prefix, or on a routing cleanup pass |
| 11  | provider-registry-and-onboarding plan, T7 review | **Duplicated `-custom`/`custom-gateway` condition** in `settings.js`'s `updateModelVisibility()`: the same check that shows `customModelGroup` is recomputed a few lines later to decide whether to auto-expand the Advanced disclosure. Not a bug, just worth hoisting into one shared boolean next time this function is touched. | `settings.js` (`updateModelVisibility()`) | Next touch of `updateModelVisibility()` |
| 12  | provider-registry-and-onboarding plan, T5 review | **`SUPPORTED_LOCALES` duplicated** between `settings.js` and `tests/settings.test.js` (the test file hand-copies the same 10 locale codes since `settings.js` doesn't export the constant). Copy-paste risk only if the supported-locale list changes — the test list would silently go stale. | `settings.js`, `tests/settings.test.js` | Next time a locale is added to or removed from `SUPPORTED_LOCALES` |
